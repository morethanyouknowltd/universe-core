import React from 'react'
export default function Layout({ children }) {
  return (
    <div className="layout">
      <div className="layout__content">{children}</div>
      {/* current route */}
      {/* actions */}
      {/* state client/server */}
      {/* available scripts, tests, record etc */}
      {/* available api calls? */}
      {/* overview of all modules */}
      {/* repl/console */}
    </div>
  )
}
